# iOS app — not deployed to Vercel (API + static `website/` only)
Guilty Pleasure Treats

# Never upload git data or backups (thousands of files → CLI upload timeouts)
.git
.git.bak

# Local / editor
.cursor
.DS_Store
*.log

# Dependencies (usually ignored by default; explicit is safe)
node_modules

# Secrets / local env (never upload)
.env
.env.*
!.env.example
